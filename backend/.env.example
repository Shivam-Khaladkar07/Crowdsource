PORT=4000
JWT_SECRET=change-me-in-production-sih-demo
# Optional: use a real PostgreSQL instance instead of embedded PGlite
# DATABASE_URL=postgres://user:pass@localhost:5432/six_jh
# Optional local embedded database directory. Do not run more than one process per directory.
# PGLITE_DATA_DIR=./data/pglite
UPLOAD_DIR=./uploads
DEMO_ENV=true
